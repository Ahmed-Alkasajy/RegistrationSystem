@page
@model student_online_system.Pages.Instructor.MyCoursesModel
@{
    ViewData["Title"] = "My Courses";
}

<div class="card shadow-sm">
    <div class="card-body p-4">
        <h3 class="mb-3">My Courses</h3>

        <div id="tableContainer">
            <p class="text-muted">Loading...</p>
        </div>
    </div>
</div>

<script>
    async function loadCourses() {
        const res = await fetch('/api/instructor/courses');
        const data = await res.json();
        const container = document.getElementById('tableContainer');

        if (data.length === 0) {
            container.innerHTML = '<p class="text-muted">You are not assigned to any courses.</p>';
            return;
        }

        container.innerHTML = `
            <table class="table table-hover">
                <thead>
                    <tr><th>Code</th><th>Name</th><th>Department</th><th>Capacity</th><th></th></tr>
                </thead>
                <tbody>
                    ${data.map(c => `
                        <tr class="hover-option">
                            <td>${c.courseCode}</td>
                            <td>${c.courseName}</td>
                            <td>${c.departmentName}</td>
                            <td>${c.capacity}</td>
                            <td><a class="btn btn-outline-light btn-sm" href="/Instructor/CourseStudents?courseId=${c.courseId}">Students</a></td>
                        </tr>`).join('')}
                </tbody>
            </table>`;
    }

    loadCourses();
</script>
